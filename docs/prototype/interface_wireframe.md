# Прототип интерфейса LongConspectWriter

## Назначение

LongConspectWriter помогает пользователю получить структурированный Markdown-конспект из аудио- или видеозаписи лекции. Интерфейс должен скрывать сложность CLI-пайплайна и показывать пользователю понятный путь: загрузить файл, настроить обработку, дождаться результата, проверить и скачать конспект.

## Основные пользователи

| Пользователь | Цель |
| --- | --- |
| Студент | Быстро получить конспект лекции из аудиозаписи |
| Преподаватель | Подготовить структурированный материал по записи занятия |
| Разработчик/исследователь | Запустить отдельные этапы пайплайна и проверить промежуточные артефакты |

## Карта экранов

```mermaid
flowchart LR
    A["Главный экран<br/>Новый конспект"] --> B["Экран обработки<br/>Прогресс пайплайна"]
    B --> C["Экран результата<br/>Просмотр и экспорт"]
    A --> D["История конспектов"]
    C --> D
    A --> E["Настройки моделей"]
    E --> A
    B --> F["Ошибка обработки"]
    F --> A
```

## Экран 1. Новый конспект

Экран нужен для загрузки лекции и выбора базовых параметров обработки.

```mermaid
flowchart TD
    classDef header fill:#E8F1FF,stroke:#315F99,color:#111
    classDef title fill:#F8F8F4,stroke:#6B7280,color:#111
    classDef primary fill:#EAF7EA,stroke:#3D7A3D,color:#111
    classDef control fill:#FFF7E6,stroke:#9A6B16,color:#111
    classDef action fill:#E6F0FF,stroke:#1D4ED8,color:#111
    classDef info fill:#F3F4F6,stroke:#6B7280,color:#111

    subgraph S1["Экран 1. Новый конспект"]
        direction TB
        H1["LongConspectWriter<br/>История | Настройки"]:::header
        T1["Новый конспект<br/>Загрузите аудио или видео лекции"]:::title
        U1["Область загрузки файла<br/>Перетащить файл сюда<br/>или нажать 'Выбрать файл'<br/>Форматы: mp3, wav, mp4, m4a"]:::primary
        F1["Выбранный файл:<br/>Лекция_1_математика.mp3"]:::info

        subgraph P1["Параметры обработки"]
            direction LR
            L1["Язык аудио<br/>Автоопределение"]:::control
            M1["Режим обработки<br/>Полный пайплайн"]:::control
        end

        O1["Опции<br/>Сохранять промежуточные файлы: выключено<br/>Запустить только выбранный этап: выключено"]:::control
        B1["Начать обработку"]:::action

        H1 --> T1 --> U1 --> F1
        F1 --> L1
        F1 --> M1
        L1 --> O1
        M1 --> O1
        O1 --> B1
    end
```

### Комментарии

| Элемент | Назначение |
| --- | --- |
| Область загрузки файла | Основной способ передать системе аудио или видео |
| Язык аудио | Можно оставить автоопределение или выбрать язык вручную |
| Режим обработки | Полный пайплайн или отдельный этап для проверки |
| Сохранять промежуточные файлы | Позволяет потом открыть транскрипт, кластеры, планы и JSON |
| Начать обработку | Переход на экран прогресса |

## Экран 2. Обработка лекции

Экран показывает состояние пайплайна и текущий этап обработки.

```mermaid
flowchart TD
    classDef header fill:#E8F1FF,stroke:#315F99,color:#111
    classDef done fill:#EAF7EA,stroke:#3D7A3D,color:#111
    classDef active fill:#FFF7E6,stroke:#B45309,color:#111
    classDef wait fill:#F3F4F6,stroke:#6B7280,color:#111
    classDef action fill:#FFECEC,stroke:#B91C1C,color:#111
    classDef log fill:#FFFFFF,stroke:#6B7280,color:#111

    subgraph S2["Экран 2. Обработка лекции"]
        direction TB
        H2["LongConspectWriter<br/>Отменить обработку"]:::header
        F2["Обработка файла:<br/>Лекция_1_математика.mp3"]:::wait
        P2["Общий прогресс<br/>48%"]:::active

        subgraph Steps["Этапы пайплайна"]
            direction TB
            E21["Готово: STT, транскрибация аудио"]:::done
            E22["Готово: Drafter, очистка транскрипта"]:::done
            E23["Выполняется: Local clustering, смысловые блоки"]:::active
            E24["Ожидает: Planner, построение плана лекции"]:::wait
            E25["Ожидает: Global clustering, привязка блоков к главам"]:::wait
            E26["Ожидает: Synthesizer, генерация JSON-конспекта"]:::wait
            E27["Ожидает: Markdown export"]:::wait
        end

        Status2["Текущий статус<br/>Embedding-модель строит локальные кластеры<br/>Ожидаемое время: 2 минуты"]:::active
        Log2["Журнал<br/>12:04 STT завершен<br/>12:05 Drafter удалил нерелевантные фрагменты<br/>12:06 Запущена локальная кластеризация"]:::log

        H2 --> F2 --> P2 --> E21 --> E22 --> E23 --> E24 --> E25 --> E26 --> E27
        E23 --> Status2 --> Log2
    end
```

### Комментарии

| Элемент | Назначение |
| --- | --- |
| Общий прогресс | Дает пользователю понимание, сколько этапов уже выполнено |
| Список этапов | Показывает, где находится пайплайн и что будет дальше |
| Текущий статус | Объясняет действие системы обычным языком |
| Журнал | Нужен для диагностики и доверия к процессу |
| Отменить обработку | Возвращает пользователя на главный экран без создания результата |

## Экран 3. Результат обработки

Экран нужен для просмотра готового конспекта, проверки структуры и экспорта результата.

```mermaid
flowchart TD
    classDef header fill:#E8F1FF,stroke:#315F99,color:#111
    classDef success fill:#EAF7EA,stroke:#3D7A3D,color:#111
    classDef panel fill:#F8F8F4,stroke:#6B7280,color:#111
    classDef action fill:#E6F0FF,stroke:#1D4ED8,color:#111
    classDef warning fill:#FFF7E6,stroke:#B45309,color:#111

    subgraph S3["Экран 3. Результат обработки"]
        direction TB
        H3["LongConspectWriter<br/>Новый конспект | История"]:::header
        R3["Конспект готов<br/>Файл: Лекция_1_математика.mp3"]:::success

        subgraph Content3["Рабочая область результата"]
            direction LR
            Tree3["Структура лекции<br/>1. Введение<br/>2. Основные определения<br/>3. Теоремы и примеры<br/>4. Итоги<br/><br/>Показать артефакты"]:::panel
            Preview3["Предпросмотр Markdown<br/>Заголовок: Введение<br/>Лекция начинается с постановки задачи...<br/><br/>Раздел: Основные определения<br/>В этом разделе вводятся..."]:::panel
        end

        Actions3["Действия<br/>Скачать .md | Скачать JSON | Открыть в редакторе"]:::action
        Warn3["Предупреждение<br/>Конспект создан AI-системой. Формулы, вычисления и термины нужно проверить."]:::warning

        H3 --> R3
        R3 --> Tree3
        R3 --> Preview3
        Tree3 --> Actions3
        Preview3 --> Actions3
        Actions3 --> Warn3
    end
```

### Комментарии

| Элемент | Назначение |
| --- | --- |
| Структура лекции | Быстрая навигация по главам итогового конспекта |
| Предпросмотр Markdown | Позволяет проверить результат до скачивания |
| Скачать .md | Сохраняет итоговый Markdown-файл |
| Скачать JSON | Скачивает структурированный результат для повторной обработки |
| Показать артефакты | Открывает промежуточные файлы: транскрипт, кластеры, планы |
| Предупреждение | Напоминает, что AI-результат требует проверки человеком |

## Экран 4. История конспектов

Экран помогает вернуться к ранее обработанным лекциям.

```mermaid
flowchart TD
    classDef header fill:#E8F1FF,stroke:#315F99,color:#111
    classDef control fill:#FFF7E6,stroke:#9A6B16,color:#111
    classDef ready fill:#EAF7EA,stroke:#3D7A3D,color:#111
    classDef error fill:#FFECEC,stroke:#B91C1C,color:#111
    classDef action fill:#E6F0FF,stroke:#1D4ED8,color:#111

    subgraph S4["Экран 4. История конспектов"]
        direction TB
        H4["LongConspectWriter<br/>Новый конспект"]:::header
        Search4["Поиск по названию лекции<br/>Фильтр: все"]:::control

        subgraph Table4["Список конспектов"]
            direction TB
            Row41["Лекция_1_математика.mp3<br/>21.04.2026 12:10<br/>Статус: готово<br/>Действие: открыть"]:::ready
            Row42["physics_intro.mp4<br/>20.04.2026 18:42<br/>Статус: готово<br/>Действие: открыть"]:::ready
            Row43["cryptography.wav<br/>19.04.2026 09:15<br/>Статус: ошибка<br/>Действие: повторить"]:::error
        end

        H4 --> Search4
        Search4 --> Row41
        Search4 --> Row42
        Search4 --> Row43
    end
```

### Комментарии

| Элемент | Назначение |
| --- | --- |
| Поиск | Быстро находит конспект по названию исходного файла |
| Фильтр | Позволяет показать только готовые, ошибочные или выполняющиеся задачи |
| Открыть | Переход на экран результата |
| Повторить | Возвращает пользователя на экран нового конспекта с тем же файлом |

## Экран 5. Настройки моделей

Экран предназначен для разработчика или продвинутого пользователя.

```mermaid
flowchart TD
    classDef header fill:#E8F1FF,stroke:#315F99,color:#111
    classDef section fill:#F8F8F4,stroke:#6B7280,color:#111
    classDef control fill:#FFF7E6,stroke:#9A6B16,color:#111
    classDef action fill:#E6F0FF,stroke:#1D4ED8,color:#111

    subgraph S5["Экран 5. Настройки моделей"]
        direction TB
        H5["LongConspectWriter<br/>Назад | Сохранить"]:::header

        subgraph STT5["STT"]
            direction LR
            STTModel5["Модель<br/>large-v3-turbo"]:::control
            Device5["Устройство<br/>cuda"]:::control
        end

        LLM5["LLM-агенты<br/>Путь к GGUF модели:<br/>.models/T-lite-it-2.1-Q5_K_M.gguf"]:::section

        subgraph Emb5["Embedding-модели"]
            direction LR
            LocalEmb5["Local clustering<br/>cointegrated/rubert-tiny2"]:::control
            GlobalEmb5["Global clustering<br/>intfloat/multilingual-e5-small"]:::control
        end

        Paths5["Директории вывода<br/>data/example-transcrib/<br/>data/example-conspect/<br/>data/example-final-conspect/"]:::section

        H5 --> STTModel5
        H5 --> Device5
        STTModel5 --> LLM5
        Device5 --> LLM5
        LLM5 --> LocalEmb5
        LLM5 --> GlobalEmb5
        LocalEmb5 --> Paths5
        GlobalEmb5 --> Paths5
    end
```

### Комментарии

| Элемент | Назначение |
| --- | --- |
| STT-настройки | Управляют моделью транскрибации и устройством выполнения |
| Путь к GGUF | Указывает локальные веса LLM-агентов |
| Embedding-модели | Отвечают за локальную и глобальную кластеризацию |
| Директории вывода | Показывают, куда сохраняются промежуточные и итоговые файлы |

## Переходы между экранами

```mermaid
flowchart TD
    Start["Новый конспект"] -->|Начать обработку| Processing["Обработка лекции"]
    Processing -->|Пайплайн завершился успешно| Result["Результат обработки"]
    Processing -->|Произошла ошибка| Error["Экран ошибки"]
    Error -->|Повторить или выбрать новый файл| Start
    Result -->|Новый конспект| Start
    Result -->|Показать артефакты| Artifacts["Промежуточные файлы"]
    Start -->|История| History["История конспектов"]
    Result -->|История| History
    History -->|Открыть готовый конспект| Result
    History -->|Повторить обработку| Start
    Start -->|Настройки| Settings["Настройки моделей"]
    Settings -->|Сохранить или назад| Start
```

## Минимальный набор для реализации MVP

Для первой версии интерфейса достаточно трех экранов:

1. Новый конспект.
2. Обработка лекции.
3. Результат обработки.

Историю и настройки можно добавить позже, потому что они улучшают удобство, но не являются обязательными для полного пользовательского сценария.
